const express=require('express');
const cors=require('cors');
const fetch=require('node-fetch');
const app=express();

app.use(cors());

app.get('/swiggy-api',async(req,res)=>{
    try{
        const response= await fetch()
    }catch(error){

    }
});